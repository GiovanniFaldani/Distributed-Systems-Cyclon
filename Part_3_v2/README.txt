HOW TO COMPILE:

c(utils).
c(server).
c(my_peer).
c(byzantine).
c(nega_byzantine).
server:run([number of peers chosen], [window_size], [L], [percentage of byzantines], [true/false for having the peers verbose or not]).